<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>home</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: 'Arial', sans-serif;
        background-color: #e0f7d5;
      }

      .container {
        text-align: center;
        position: relative;
        top: 250px;
      }

      h1 {
        color: #228b22;
      }

      .button {
        display: inline-block;
        padding: 10px 20px;
        margin: 10px;
        font-size: 16px;
        text-align: center;
        text-decoration: none;
        border-radius: 5px;
        cursor: pointer;
      }

      #signupButton,
      #signinButton,
      #signoutButton {
        background-color: #4caf50;
        color: white;
        position: absolute;
        top: 10px;
      }

      #signupButton {
        right: 10px;
      }

      #signinButton {
        right: 130px;
      }

      #signoutButton {
        right: 20px;
        display: none;
      }

      #startButton {
        padding: 20px 40px;
        background-color: #008cba;
        color: white;
        margin-top: 20px;
      }

      .rolling {
        text-align: center;
        position: relative;
        top: 350px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>자라나라 새싹가든</h1>
    </div>

    <a href="signup.ejs" class="button" id="signupButton">회원가입</a>
    <a href="signin.ejs" class="button" id="signinButton">로그인</a>
    <a class="button" id="signoutButton">로그아웃</a>

    <div class="rolling">
      <a href="garden.ejs" class="button" id="startButton"
        >롤링페이퍼 시작하기</a
      >
    </div>

    <!-- 링크는 편의를 위해 걸어두었으며, -->
    <!-- 로그인 여부 서버에서 확인 뒤 -->
    <!-- 비로그인 시 : 시작하기 버튼 누르면 로그인 페이지로 render -->
    <!-- 로그인 시 : 회원가입, 로그인 버튼 hide 하고 로그아웃 버튼 나타남. 시작하기 버튼 누르면 garden render -->
    <!-- 되는 코드 작성 필요 -->
    <script></script>
  </body>
</html>
